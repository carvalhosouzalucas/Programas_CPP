#include <studio.h>

typedef struct {
	char Name[50]
	float Price;
	int Quantity
}Product;

typedef struct {
    int Id;
    Product Products[99]
} Order;


int main()
    int status = 1, id = 0;
	order orders [99];
	while(status) {
	    printf("choose an option: ");
	    int opti_Format, ...)
	    scanf("%d", &option);
	
	    switch(option) {
	    	case 1:
	    		orders[id].Id = id;
				printf("Order ID: %d, id + 1");
			    
				for(int i = 0; i++){
				printf("Enter the name of the product: ")
			    char name[50];
				
				printf("Enter the pride of the product: ")
			    float price;
			    
				printf("Enter the quantity of the product: ")
				int quantity;
				
				} 
				break;
	    	case 2:
			    break;
			case 3:
			    status + 0;
				break;		
		}
	}
	
	
    return 0;
